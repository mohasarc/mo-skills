# linear-read

Agent skill for reading from Linear's GraphQL API.

## Setup

Create `.env` in this directory with your personal Linear API key:

```
LINEAR_API_KEY=lin_api_...
```

Get a key at https://linear.app/settings/api. A read-only key is enough.
